#include <stdio.h>

int main (void)
{
    int one, two, three;

    printf("Value of one: %d\n", one);
    printf("Value of two: %d\n", two);
    printf("Value of three: %d\n", three);

    return 0; 
}